package com.designguru.code.pattern.simulation;

/**
 *
 * You are given four positive integers rows, cols, rStart,
 * and cStart. You start at cell (rStart, cStart) in a grid of size rows x cols.
 * The grid's top-left corner is at (0, 0) and the bottom-right corner is at (rows-1, cols-1).
 *
 * You need to visit every cell of this grid by walking in a clockwise spiral pattern.
 * If you move outside the grid's boundaries, you continue walking outside but will return to the grid later.
 *
 * Return an array of coordinates representing the order of cells visited during this walk.
 *
 * 定义 方向作为状态
 *
 */
public class SpiralMatrix {

    public int[][] spiralMatrixIII(int rows, int cols, int rStart, int cStart) {
        int[][] result = new int[rows * cols][2];
        // ToDo: Write Your Code Here.
        return result;
    }

}
